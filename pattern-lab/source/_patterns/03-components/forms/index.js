// const schema = require('./cta-schema.json');

const meta = {
  id: 'cta',
  title: 'form',
  type: 'component',
  template: '@components/_cta.twig',
  schema: {},
};

module.exports = {
  meta,
};
