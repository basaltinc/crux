{
  "$schema": "http://json-schema.org/draft-04/schema",
  "type": "object",
  "title": "Media Tile",
  "description": "Full Image overlayed with optional Title, Body, and Buttons.",
  "properties": {
    "title": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "text_align": {
      "type": "string",
      "enum": ["left", "center", "right"]
    },
    "text_align_vert": {
      "type": "string",
      "enum": ["top", "center", "bottom"]
    },
    "title_bg_color": {
      "type": "string",
      "title": "Title Background Color",
      "description": "Any utility color class"
    },
    "title_size": {
      "type": "string",
      "description": "Title size corresponds to sitewide heading sizes. 1 = h1, 2 = h2, etc.",
      "enum": ["1", "2", "3", "4", "5", "6"]
    },
    "title_text_color": {
      "type": "string",
      "description": "Any utility color class"
    },
    "body": {
      "type": "string"
    },
    "body_text_color":{
      "type": "string",
      "title": "Body Text Color",
      "description": "Any utility color class"
    },
    "body_text_size": {
      "type": "string",
      "enum": ["s", "m", "l", "xl", "xxl"]
    },
    "content_padding": {
      "type": "string",
      "description": "Space around the content in relation to the media",
      "enum": ["0", "s", "m", "l", "xl", "xxl"]
    },
    "media": {
      "type": "string",
      "title": "Background Image",
      "description": "Path to image."
    },
    "image_overlay": {
      "type": "string",
      "enum": ["blue", "green", "yellow", "black", "none"]
    },
    "min_height": {
      "type": "string",
      "title": "Minimum Element Height",
      "enum": ["0", "xxs", "xs", "s", "m", "l", "xl", "xxl"]
    },
    "bg_color": {
      "type": "string",
      "title": "Background Color (if no image)",
      "enum": ["blue", "green", "yellow", "iron", "black", "none"]
    },
    "media_padding": {
      "type": "string",
      "description": "Space around the media",
      "enum": ["0", "s", "m", "l", "xl", "xxl"]
    },
    "buttons": {
      "type": "array",
      "description": "List of objects passed to @components/_button.twig"
    },
    "list": {
      "type": "array",
      "description": "List of objects passed to @components/_list.twig"
    }
  },
  "required": ["text"]
}
