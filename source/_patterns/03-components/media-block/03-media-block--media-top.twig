{% include '@components/_media-block.twig' with {
  title: random(titles),
  body: random(paragraphs),
  media: '../../../../images/brand-stock/' ~ random(get_file_list('images/brand-stock/*.jpg')),
  media_alignment: 'top',
} %}
